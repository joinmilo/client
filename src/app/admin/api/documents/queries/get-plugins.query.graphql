query getPlugins($params: FilterSortPaginateInput) {
  getPlugins(params: $params) {
    result {
      id
      active
      code
      icon
      name
      released
      menuItems {
        id
      }
      translatables {
        id
        name
        language {
          ...Language
        }
      }
    }
    total
  }
}